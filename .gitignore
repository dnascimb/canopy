__pycache__/
*.py[cod]
.venv/
venv/
instance/
*.db
*.sqlite
.env
.pytest_cache/
.ruff_cache/
htmlcov/
.coverage
dist/
build/
*.egg-info/
.DS_Store

# Personal collection notes — never commit
collection-index.md
Cannabis/

# Data backups and DB snapshots — the garden's real data, never in the repo.
# `flask --app wsgi export-json` output belongs outside the working tree; these
# patterns are a backstop for when it is written inside it by accident.
canopy-backup*.json
canopy-db*.sqlite
backups/
*.bak
